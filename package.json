{
  "name": "ayancurtains-admin",
  "version": "1.0.0",
  "description": "Admin panel for Ayan Curtains",
  "scripts": {
    "start": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install-deps": "npm install && cd client && npm install && cd ../server && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
